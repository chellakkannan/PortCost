@model IEnumerable<PortCostApp.Core.Vessels>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.VesselName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.VesselName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.VesselCode }) |
            @Html.ActionLink("Details", "Details", new { id=item.VesselCode }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.VesselCode })
        </td>
    </tr>
}

</table>
